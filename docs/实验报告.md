# 灵感笔记 HarmonyOS 应用实验报告

## 一、项目概述

### 1.1 项目基本信息

- 项目名称：灵感笔记（todolistAPP）
- 应用包名：`com.example.todolistapp`
- 开发平台：HarmonyOS
- SDK 版本：6.0.2（API 22）
- 开发语言：ArkTS、C++
- 构建工具：Hvigor、CMake、Ninja
- 工程模式：Stage Model
- 目标设备：手机、平板

灵感笔记是一款面向个人用户的轻量级笔记与待办管理应用。应用核心目标是降低用户记录和整理信息的成本：用户输入会议记录、学习笔记、生活备忘或临时想法后，系统可以自动提取关键词、推荐标签、生成摘要，并在文本中存在明确行动项时生成待办事项。

项目采用“本地优先、云端辅助”的 AI 整理策略。本地 AI 由 ArkTS 规则逻辑与 C++ Native 模块共同实现，可在离线场景下完成基础分析；云端 AI 通过通义千问兼容接口补充更强的生成能力。实验重点围绕 Native 原生增强和全场景适配展开，完成了 C++ 文本分析、C++ 密码加密、响应式布局和平板分栏四个方向。

### 1.2 实验目标

本实验主要完成以下目标：

1. 使用 HarmonyOS ArkTS 实现笔记、待办、搜索、图片、用户登录注册等基础功能。
2. 将核心文本分析能力迁移到 C++，通过 NAPI 暴露给 ArkTS 调用，改善中文关键词提取效果。
3. 将用户密码存储由明文改为 C++ SHA-256 加盐哈希，提高本地数据安全性。
4. 面向手机和平板实现响应式布局，避免简单等比拉伸。
5. 在平板设备上实现列表-详情双栏结构，提高大屏使用效率。
6. 完成集成测试、HapTest 测试方案整理和实验报告编写。

## 二、技术栈与工程结构

### 2.1 技术栈

| 类型 | 技术/模块 | 用途 |
| --- | --- | --- |
| 系统平台 | HarmonyOS Stage Model | 应用生命周期与页面管理 |
| 开发语言 | ArkTS | 页面、组件、业务逻辑 |
| Native 语言 | C++17 | 文本分析和密码加密 |
| Native 桥接 | NAPI | ArkTS 与 C++ 通信 |
| 构建工具 | Hvigor、CMake、Ninja | HAP 与 Native 动态库构建 |
| 数据存储 | `@ohos.data.relationalStore` | RDB 笔记/待办持久化 |
| 偏好存储 | `@ohos.data.preferences` | 用户账号、登录状态保存 |
| 图片能力 | PhotoViewPicker | 选择笔记图片 |
| 网络能力 | `@ohos.net.http` | 云端 AI 请求 |
| 测试方案 | HapTest 用例设计、本机构建验证 | 集成与多端适配验证 |

### 2.2 工程结构

项目主模块为 `entry`，核心目录如下：

```text
todolistAPP/
├── .ai/                                  # 项目说明、分工、测试用例
├── docs/                                 # 测试报告与实验报告
├── entry/
│   ├── build-profile.json5               # 模块构建配置，含 Native CMake 配置
│   └── src/main/
│       ├── cpp/                          # C++ Native 模块
│       │   ├── CMakeLists.txt
│       │   ├── text_analyzer.cpp
│       │   ├── crypto_utils.cpp
│       │   └── napi_init.cpp
│       ├── ets/
│       │   ├── common/                   # 常量、主题、断点枚举
│       │   ├── components/               # NoteCard、TagChip 等组件
│       │   ├── database/                 # RDB 与 Preferences 工具
│       │   ├── entryability/             # EntryAbility 初始化
│       │   ├── model/                    # Note、Todo、Tag 模型
│       │   ├── pages/                    # 登录、首页、编辑、搜索等页面
│       │   ├── service/                  # NoteService、ImageService
│       │   ├── utils/                    # Native 封装、断点工具等
│       │   └── viewmodel/                # UserViewModel、NoteViewModel
│       └── resources/                    # 图片、颜色、字符串、路由配置
└── build-profile.json5                   # 工程级构建配置
```

## 三、代码设计

### 3.1 分层架构

项目采用清晰的分层结构：

```text
用户操作
  ↓
Pages 页面层
  ↓
Components 组件层
  ↓
ViewModel 视图模型层
  ↓
Service 业务服务层
  ↓           ↘
Database 数据层  Native C++ 模块
```

各层职责如下：

| 层级 | 代表文件 | 主要职责 |
| --- | --- | --- |
| 页面层 | `LoginPage.ets`、`Index.ets`、`NoteEditPage.ets` | 页面状态、用户交互、路由跳转、平板嵌入显示 |
| 组件层 | `NoteCard.ets`、`TagChip.ets`、`TodoStatusSwitch.ets` | 可复用 UI 组件 |
| ViewModel | `UserViewModel.ets`、`NoteViewModel.ets` | 登录注册逻辑、列表数据转换 |
| Service | `NoteService.ets`、`ImageService.ets` | 笔记 CRUD、本地 AI、云 AI、图片选择 |
| Native | `text_analyzer.cpp`、`crypto_utils.cpp` | C++ 文本分析、密码加密 |
| 数据层 | `RdbStoreManager.ets`、`PreferenceUtil.ets` | 数据库表创建、迁移、查询、偏好存储 |
| 模型层 | `Note.ets`、`Todo.ets` | 数据结构定义 |

### 3.2 数据模型设计

项目主要使用 `notes` 和 `todos` 两张表。

`notes` 表保存笔记和待办的通用字段，包括标题、正文、图片、标签、AI 摘要、行动项、优先级、创建时间、是否待办和待办状态等。普通笔记与待办通过 `isTodo` 字段区分。

`todos` 表保存待办详情字段，包括关联笔记 ID、状态、截止时间和优先级。该设计方便后续扩展提醒、截止日期和更多待办属性。

`Note` 模型中核心字段包括：

```typescript
interface Note {
  id: string
  userId?: string
  title: string
  content: string
  imagePath: string
  tags: string
  aiTags?: string
  summary?: string
  actionItems?: string
  priority?: number
  createTime: number
  isTodo: number
  todoStatus: TodoVisualStatus
}
```

### 3.3 本地 AI 数据流

本地 AI 整理流程如下：

1. 用户在 `NoteEditPage` 输入标题和正文。
2. 点击“本地 AI 自动提取”。
3. 页面调用 `NoteService.generateLocalAiSuggestion(title, content)`。
4. `NoteService` 调用 Native 文本分析模块提取关键词和摘要候选句。
5. ArkTS 规则层继续完成标签推荐、摘要压缩、行动项抽取、时间识别和优先级推断。
6. 页面展示关键词、推荐标签、摘要和待办行动项。
7. 如果存在明确行动项，用户可选择保存为待办；如果是纯记录，则不强制生成待办。

## 四、功能实现

### 4.1 用户系统

用户系统由 `LoginPage`、`RegisterPage` 和 `UserViewModel` 实现。用户注册时输入账号、昵称和密码，系统调用 Native 加密模块生成随机盐值并计算 SHA-256 哈希，最终在 Preferences 中保存 `hash:salt` 格式记录，而不是明文密码。

登录时，`UserViewModel` 从 Preferences 读取用户记录。如果记录已经是 `hash:salt`，则调用 `NativeCrypto.verifyPassword()` 完成校验；如果检测到旧版明文记录，则在密码正确时自动迁移为加盐哈希。

### 4.2 笔记与待办管理

笔记编辑页支持标题、正文、图片、标签、AI 推荐标签、摘要和行动项。用户保存后，`NoteService` 将数据写入 RDB。首页通过 `NoteViewModel` 查询并转换为卡片数据展示。

待办支持三态管理：

- 待处理
- 已完成
- 已延期

`NoteCard` 中提供 Checkbox 和状态胶囊，`TodoDetailPage` 中提供更完整的待办编辑能力。状态变更后通过 `RdbStoreManager` 同步到数据库，列表与详情页保持一致。

### 4.3 搜索与筛选

搜索页支持关键词搜索、日期范围筛选和标签筛选。搜索结果复用 `NoteCard` 展示，待办状态可以在搜索结果中直接操作。宽屏下搜索页限制最大宽度并居中，搜索结果使用响应式网格展示。

### 4.4 图片功能

`ImageService` 封装 PhotoViewPicker 和图片权限申请。编辑页可选择或清除图片，`NoteCard` 根据断点调整图片高度，使手机和平板显示比例更合理。

### 4.5 本地 AI 整理功能

本地 AI 功能包括：

- 关键词提取
- 推荐标签
- 摘要生成
- 行动项提取
- 时间识别
- 优先级推断

其中关键词提取和句子评分由 C++ Native 模块实现，推荐标签、摘要压缩和行动项规则由 ArkTS 实现。近期迭代中修复了多个边界问题，例如：

- `明天记得买咖啡` 不再生成 `天记` 标签。
- 生活购物场景中不再出现 `备去`、`要买` 等跨词 n-gram 噪声。
- 多个行动项会生成多个待办预览卡片。
- 纯记录类文本不再强制生成待办。

### 4.6 云端 AI 整理功能

云端 AI 通过 `HttpUtil.postJson()` 调用通义千问兼容接口，请求格式类似 OpenAI Chat Completions。`NoteService` 负责构造结构化 Prompt，并要求云端返回 JSON：

```json
{
  "summary": "...",
  "keywords": ["..."],
  "actionItems": ["..."],
  "suggestedTags": ["..."]
}
```

解析逻辑支持直接 JSON、`data` 嵌套格式以及 `choices[0].message.content` 格式。如果云端请求失败，页面显示错误提示，本地 AI 能力仍可继续使用。

## 五、跨语言 Native 实现

### 5.1 C++ 构建配置

模块级 `entry/build-profile.json5` 添加了 Native 构建配置：

```json5
"externalNativeOptions": {
  "path": "./src/main/cpp/CMakeLists.txt",
  "arguments": "",
  "cppFlags": "",
  "abiFilters": [
    "arm64-v8a",
    "x86_64"
  ]
}
```

`CMakeLists.txt` 构建共享库 `text_analyzer`：

```cmake
add_library(text_analyzer SHARED
    text_analyzer.cpp
    crypto_utils.cpp
    napi_init.cpp
)

target_link_libraries(text_analyzer PUBLIC
    libace_napi.z.so
    libhilog_ndk.z.so
)
```

同时配置 `arm64-v8a` 和 `x86_64`，解决真机和模拟器 ABI 不一致导致的安装失败问题。

### 5.2 C++ 文本分析模块

`text_analyzer.cpp` 完成以下工作：

1. UTF-8 解码，确保中文字符边界正确。
2. 对中文文本进行 2-gram 和 3-gram 切分。
3. 过滤标点、纯数字、长度过短、停用词和跨词边界噪声。
4. 计算 TF-IDF 风格权重，结合预设 IDF 提升领域词权重。
5. 按权重排序返回 top K 关键词。
6. 将文本按句号、问号、感叹号、分号等切分句子。
7. 根据句长、关键词命中、摘要暗示词和长度惩罚对句子评分。
8. 返回得分最高的句子，供 ArkTS 层进一步压缩为摘要。

### 5.3 C++ 密码加密模块

`crypto_utils.cpp` 实现：

- SHA-256 哈希
- 16 字节随机盐值生成
- 十六进制编码
- 密码验证
- 常量时间字符串比较

密码存储格式为：

```text
64位hex哈希:32位hex盐值
```

该实现避免明文密码落盘，并兼容旧版本明文账号迁移。

### 5.4 NAPI 桥接

`napi_init.cpp` 注册了以下 Native 方法：

| 方法 | ArkTS 调用 | 功能 |
| --- | --- | --- |
| `extractKeywords` | `NativeTextAnalyzer.extractKeywords()` | 提取关键词 |
| `scoreSentences` | `NativeTextAnalyzer.scoreSentences()` | 评分摘要候选句 |
| `hashPassword` | `NativeCrypto.hashPassword()` | 计算密码哈希 |
| `generateSalt` | `NativeCrypto.generateSalt()` | 生成随机盐值 |
| `verifyPassword` | `NativeCrypto.verifyPassword()` | 验证密码 |

ArkTS 通过如下方式导入动态库：

```typescript
import textAnalyzer from 'libtext_analyzer.so'
```

然后在 `NativeTextAnalyzer.ets` 和 `NativeCrypto.ets` 中封装异常处理，确保 Native 调用失败时不会导致页面崩溃。

### 5.5 跨语言实现效果

跨语言实现带来的效果包括：

1. 中文关键词不再依赖空格切分，能够在无空格中文文本中提取有效关键词。
2. 文本分析逻辑从 ArkTS 迁移到 C++ 后，便于后续优化算法和复用。
3. 密码加密由 C++ 实现，减少 ArkTS 侧明文处理时间，提高安全性。
4. 通过 NAPI 将 Native 能力封装成 ArkTS 工具类，业务层调用方式保持简洁。

## 六、跨设备实现与效果

### 6.1 响应式断点系统

项目新增 `Breakpoint` 枚举和 `ResponsiveConstants` 常量，断点分为：

| 断点 | 宽度范围 | 布局策略 |
| --- | --- | --- |
| SM | 0-599vp | 手机单列 |
| MD | 600-839vp | 小平板/横屏，两列或居中表单 |
| LG | 840vp 及以上 | 平板三列或列表-详情双栏 |

`BreakpointUtil` 使用 `AppStorage` 保存全局断点状态，并提供页面 padding、网格列数、图片高度、文本预览行数等辅助方法。`EntryAbility` 在窗口创建和尺寸变化时更新断点，页面通过 `@StorageProp('currentBreakpoint')` 获取当前断点。

### 6.2 手机端效果

手机端保持单栏体验：

- 首页使用 Tab 区分“我的笔记”和“待办事项”。
- 笔记卡片单列展示。
- 点击卡片通过路由进入独立编辑页。
- 新建笔记和新建待办通过独立页面完成。
- 页面宽度充分利用屏幕，避免多栏导致内容拥挤。

### 6.3 平板端效果

LG 断点下首页使用列表-详情结构：

- 左侧栏宽度约 360vp，展示筛选区、笔记列表和待办列表。
- 右侧区域嵌入 `NoteEditPage` 或 `TodoDetailPage`。
- 点击左侧卡片不会跳转页面，而是直接切换右侧详情。
- 编辑标题、正文、图片和标签时，左侧卡片可以实时更新预览。
- 保存或删除后刷新列表并保持合理的选中状态。

### 6.4 跨设备关键实现

项目在跨设备实现中解决了以下问题：

1. 不能简单等比拉伸：通过断点控制列数、padding、表单最大宽度和图片高度。
2. 手机与平板导航方式不同：手机使用路由跳转，平板使用嵌入编辑。
3. 平板左侧列表需要实时预览草稿：通过 `onDraftChange` 回调更新父组件状态。
4. ArkUI `ForEach` 复用导致卡片不刷新：将标题、正文、图片和标签纳入 key。
5. ArkUI V1 `@Prop` 不适合函数回调：回调改为普通组件属性。

## 七、开发过程中遇到的问题及解决方法

### 7.1 中文关键词切分效果差

问题：最初本地 AI 使用空格切分文本。中文文本通常没有空格，导致整段内容被当成一个词，关键词几乎不可用。

解决：使用 C++ 实现 UTF-8 中文 n-gram 分词，并加入停用词过滤、领域词权重和 TF-IDF 风格排序。ArkTS 层通过 NAPI 调用 Native 模块。

### 7.2 Native 编译中 `LOG_DOMAIN` 和 `LOG_TAG` 冲突

问题：`hilog/log.h` 中已经定义 `LOG_DOMAIN` 和 `LOG_TAG` 宏，C++ 文件中使用同名常量后被宏替换，导致编译失败。

解决：将自定义日志常量改名为 `TEXT_ANALYZER_LOG_DOMAIN` 和 `TEXT_ANALYZER_LOG_TAG`，避免宏冲突。

### 7.3 安装时报 ABI 不匹配

问题：Native 动态库最初只生成 `arm64-v8a`，在 x86_64 模拟器上安装时报 “Abi type supported by the device does not match”。

解决：在 `entry/build-profile.json5` 中增加：

```json5
"abiFilters": ["arm64-v8a", "x86_64"]
```

并清理 `.cxx` 缓存后重新构建。

### 7.4 平板点击卡片闪退

问题：平板分栏中给子组件传递 `onSaved`、`onDeleted`、`onDraftChange` 回调时，使用了 `@Prop`，ArkUI V1 不支持函数类型 `@Prop`，导致运行时崩溃。

解决：将函数回调改为普通组件属性，避免状态装饰器处理函数对象。

### 7.5 平板左侧卡片不实时刷新

问题：编辑右侧详情时，左侧卡片没有立即更新标题和正文。

原因：`ForEach` key 只包含 ID 和状态，ArkUI 复用了旧节点。

解决：将标题、正文、图片、标签等预览字段纳入 key，草稿变化后强制刷新对应卡片。

### 7.6 多待办只显示一个预览

问题：AI 提取出多个行动项后，预览区域只显示一个待办卡片，用户以为只会保存一条。

解决：`NoteEditPage` 的预览区域改为复用 `buildGeneratedTodoInputs()`，通过 `ForEach` 展示多张待办卡片；按钮文案改为“保存 N 条待办事项”。

### 7.7 纯记录文本被强制生成待办

问题：读书笔记、观点摘录等纯记录文本没有明确行动项，但旧逻辑会把原文作为待办。

解决：删除行动项抽取的整句 fallback。页面层也不再使用摘要或正文兜底创建待办。若行动项为空，按钮显示“无可保存待办”，并提示“未识别到明确待办事项”。

### 7.8 短文本产生奇怪标签

问题：输入“明天记得买咖啡”时，n-gram 切出 `天记` 这类跨词边界片段，并进入推荐标签。

解决：在 C++ 停用词中加入 `天记`、`明天记`、`记得买` 等跨边界噪声；ArkTS 标签层也加入兜底停用词，双层过滤。

## 八、HapTest 测试结果与验证

### 8.1 测试目标

HapTest 测试围绕前四部分集成后的主链路展开，重点验证：

1. Native 文本分析和 Native 密码加密能与 ArkTS 正常协作。
2. 登录注册、笔记编辑、待办状态、搜索筛选等核心功能可运行。
3. 手机和平板布局能够按断点正确切换。
4. 平板列表-详情分栏无崩溃，保存和删除后列表状态正确。

### 8.2 测试策略一：核心功能集成冒烟

| 用例编号 | 用例名称 | 操作步骤 | 期望结果 | 当前结果 |
| --- | --- | --- | --- | --- |
| F-01 | 注册和登录 | 注册账号，退出后重新登录 | 密码非明文保存，登录成功 | 密码加密专项已通过 |
| F-02 | 新建笔记 | 填写标题、正文、标签并保存 | 首页出现新卡片 | 待真机系统执行 |
| F-03 | 本地 AI 整理 | 输入含任务和时间的正文，点击本地 AI | 生成关键词、摘要和行动项 | 多轮手工验证通过，HapTest 待执行 |
| F-04 | 待办三态同步 | 在列表和详情页切换状态 | 卡片角标和状态同步 | 待真机系统执行 |
| F-05 | 搜索筛选 | 按关键词、日期、标签筛选 | 只显示匹配记录 | 待真机系统执行 |

### 8.3 测试策略二：多端适配与分栏验证

| 用例编号 | 用例名称 | 操作步骤 | 期望结果 | 当前结果 |
| --- | --- | --- | --- | --- |
| R-01 | 手机单列布局 | 手机宽度打开首页 | 列表单列展示 | 手机模拟器验证通过 |
| R-02 | 平板多列列表 | 平板宽度打开首页 | 列表 2 到 3 列 | 平板模拟器验证通过 |
| R-03 | 平板双栏编辑 | 平板点击笔记卡片 | 左列表右详情 | 平板模拟器验证通过 |
| R-04 | 窗口尺寸变化 | 跨过 600vp/840vp | padding、列数、图片高度更新 | 待系统化执行 |
| R-05 | 搜索页宽屏居中 | 平板打开搜索页 | 搜索区域居中且限宽 | 待系统化执行 |

### 8.4 Native 密码加密测试结果

| 用例 | 测试内容 | 测试结果 | 是否通过 |
| --- | --- | --- | --- |
| C1 | 新用户注册后检查 Preferences | 记录长度 97，格式为 `hash:salt`，不包含明文密码 | 通过 |
| C2 | 正确密码登录 | `Pw123456` 登录进入首页 | 通过 |
| C3 | 错误密码登录 | `Wrong123` 停留在登录页 | 通过 |
| C4 | 旧明文迁移 | 迁移路径已实现 | 待构造旧账号后补测 |

### 8.5 本机集成构建结果

已完成一次本机 HAP 构建验证：

```text
BUILD SUCCESSFUL in 11 s 25 ms
```

构建结果：

- `CompileArkTS` 通过。
- `BuildNativeWithCmake` 通过。
- `BuildNativeWithNinja` 通过。
- `PackageHap` 通过。
- `PackingCheck` 通过。
- `SignHap` 通过。
- `assembleHap` 通过。

说明：当前 `docs/haptest-report.md` 中记录的 HapTest 主链路用例仍有部分标记为“待真机执行”。因此最终提交前建议补充真机或模拟器截图、执行日期、执行人和通过状态。

## 九、成员分工

| 成员 | 负责方向 | 主要任务 | 主要文件 | 完成情况 |
| --- | --- | --- | --- | --- |
| 成员1（姓名待填） | Native 关键词提取 | C++ 文本分析、NAPI 注册、ArkTS 封装、NoteService 接入 | `text_analyzer.cpp`、`napi_init.cpp`、`NativeTextAnalyzer.ets`、`NoteService.ets` | 已完成 |
| 成员2（姓名待填） | Native 密码加密 | SHA-256、随机盐、密码验证、旧密码迁移 | `crypto_utils.cpp`、`NativeCrypto.ets`、`UserViewModel.ets` | 已完成 |
| 成员3（姓名待填） | 响应式断点系统 | SM/MD/LG 断点、网格列表、宽屏限宽、图片高度适配 | `BreakpointUtil.ets`、`Constants.ets`、`Index.ets`、`NoteCard.ets` | 已完成 |
| 成员4（姓名待填） | 平板分栏架构 | SideBarContainer、嵌入式编辑、手机/平板导航差异 | `Index.ets`、`NoteEditPage.ets`、`TodoDetailPage.ets` | 已完成 |
| 成员5（姓名待填） | 集成测试与优化 | UI 常量统一、构建验证、HapTest 方案、实验报告技术章节 | `Constants.ets`、`docs/haptest-report.md`、`docs/technical-chapter.md` | 已完成/待补真机记录 |

## 十、生成式 AI 使用报告

> 说明：本章记录开发阶段使用生成式 AI 辅助完成项目的情况。这里的“开发辅助 AI”指开发过程中使用的豆包；“应用内 AI 功能”指本项目面向用户提供的本地 AI 自动提取、云端 AI 整理等功能，两者用途和运行位置不同。

### 10.1 是否使用生成式 AI

是。本项目第 1 人 Native 关键词提取任务在开发过程中使用豆包作为开发辅助 AI，主要用于理解任务要求、梳理实现步骤、解释 TF-IDF 算法和设计自测用例。

### 10.2 使用的生成式 AI 工具

豆包。

### 10.3 AI 在开发过程中承担的任务

豆包在开发过程中承担的是开发辅助角色，不参与应用运行时功能。具体包括：

- 根据小组分工和 HarmonyOS ArkTS 技术栈，拆解第 1 人 Native 关键词提取任务。
- 给出 CMake、C++ 算法、NAPI 绑定、ArkTS 封装和 NoteService 接入的开发顺序。
- 说明 TF-IDF 算法原理、移动端简化实现方式和停用词过滤必要性。
- 给出 text_analyzer.cpp 的函数结构、分词、关键词排序和句子评分设计建议。
- 设计关键词提取、句子评分、空文本、纯停用词、长文本和中英文混合文本等测试用例。
- 提醒 NAPI 类型转换、UTF-8 字符边界、ABI 架构和模块打包等潜在风险。

### 10.4 完整对话记录

```text
五人轮流开发分工 开发顺序与依赖 第1人 → 第2人 → 第3人 → 第4人 → 第5人 (Native关键词) (Native加密) (响应式断点) (分栏架构) (集成测试) ▎ 第2人和第3人可以调换，因为 Native 加密和响应式布局互不依赖。 --- 第1人 - Native 环境搭建 + C++ 关键词提取算法 交付物: C++ TF-IDF 关键词提取模块，通过 NAPI 被 ArkTS 调用 具体任务: 1. 配置 CMakeLists.txt + 修改 build-profile.json5 添加 NDK 构建 2. 编写 text_analyzer.cpp：n-gram 中文分词 + TF-IDF 权重计算 + TextRank 句子评分 3. 编写 napi_init.cpp：NAPI 模块注册，暴露 extractKeywords() 和 scoreSentences() 4. 编写 NativeTextAnalyzer.ets：ArkTS 封装层 5. 修改 NoteService.ets：替换原有 extractKeywords() 和 buildLocalSummary() 为 Native 版本 新建文件: - entry/src/main/cpp/CMakeLists.txt - entry/src/main/cpp/text_analyzer.cpp - entry/src/main/cpp/napi_init.cpp - entry/src/main/ets/utils/NativeTextAnalyzer.ets 修改文件: - entry/build-profile.json5 - entry/src/main/ets/service/NoteService.ets --- 第2人 - C++ 密码加密存储 交付物: SHA-256 密码哈希模块，用户密码安全存储 具体任务: 1. 编写 crypto_utils.cpp：SHA-256 哈希 + 随机盐值生成 + 密码验证 2. 在 napi_init.cpp 中注册 crypto 模块，暴露 hashPassword() / generateSalt() / verifyPassword() 3. 编写 NativeCrypto.ets：ArkTS 封装层 4. 修改 UserViewModel.ets：注册时哈希存储，登录时验证，旧用户自动迁移 新建文件: - entry/src/main/cpp/crypto_utils.cpp - entry/src/main/ets/utils/NativeCrypto.ets 修改文件: - entry/src/main/cpp/CMakeLists.txt（合并构建） - entry/src/main/cpp/napi_init.cpp（注册新模块） - entry/src/main/ets/viewmodel/UserViewModel.ets --- 第3人 - 响应式布局断点系统 交付物: MediaQuery 断点系统，手机单列/平板多列自适应 具体任务: 1. 在 Constants.ets 添加 Breakpoint 枚举和 ResponsiveConstants 响应式常量 2. 新建 BreakpointUtil.ets：断点管理器，监听屏幕宽度变化 3. 改造 EntryAbility.ets：注册 MediaQuery 监听 4. 改造 Index.ets：List 改为 GridRow/GridCol，手机1列/平板2-3列 5. 改造 NoteCard.ets：根据断点调整图片高度、内容行数 6. 改造 NoteEditPage.ets / TodoDetailPage.ets / SearchResultPage.ets：平板端限制最大宽度居中 新建文件: - entry/src/main/ets/utils/BreakpointUtil.ets 修改文件: - entry/src/main/ets/common/Constants.ets - entry/src/main/ets/entryability/EntryAbility.ets - entry/src/main/ets/pages/Index.ets - entry/src/main/ets/pages/NoteEditPage.ets - entry/src/main/ets/pages/TodoDetailPage.ets - entry/src/main/ets/pages/SearchResultPage.ets - entry/src/main/ets/components/NoteCard.ets --- 第4人 - 平板分栏架构 交付物: SideBarContainer 列表-详情双栏布局 具体任务: 1. 改造 Index.ets：平板端使用 SideBarContainer，左侧列表（360vp）右侧详情 2. 改造 NoteEditPage.ets：添加 @Prop isEmbedded 支持嵌入模式（去掉 Scroll/返回按钮） 3. 改造 TodoDetailPage.ets：同上 4. 处理导航差异：手机端 router.pushUrl，平板端直接更新右侧组件状态 5. 状态同步：列表选中态、编辑内容实时预览 修改文件: - entry/src/main/ets/pages/Index.ets（主要改动） - entry/src/main/ets/pages/NoteEditPage.ets - entry/src/main/ets/pages/TodoDetailPage.ets --- 第5人 - 集成测试与优化 交付物: 完整可运行应用 + HapTest 测试报告 具体任务: 1. 集成联调：确保 Native 模块 + 多端适配协同工作 2. Bug 修复：处理前4人的集成问题 3. UI 统一：硬编码尺寸改为常量（标题28→TITLE_SIZE等） 4. HapTest 测试：选择2个测试策略完成测试报告 5. 编写实验报告技术章节 --- 3. UI 统一：硬编码尺寸改为常量（标题28→TITLE_SIZE等） 4. HapTest 测试：选择2个测试策略完成测试报告 5. 编写实验报告技术章节 --- 时间建议（截止 6.20） ┌───────┬──────────┬───────────┐ │ 人员 │ 预计天数 │ 建议日期 │ ├───────┼──────────┼───────────┤ │ 第1人 │ 3天 │ 6.9-6.11 │ ├───────┼──────────┼───────────┤ │ 第2人 │ 2天 │ 6.11-6.12 │ ├───────┼──────────┼───────────┤ │ 第3人 │ 2天 │ 6.12-6.14 │ ├───────┼──────────┼───────────┤ │ 第4人 │ 2天 │ 6.14-6.16 │ ├───────┼──────────┼───────────┤ │ 第5人 │ 3天 │ 6.16-6.20 │ └───────┴──────────┴───────────┘ 这是我们小组的任务分工我负责第一个任务，你先给我一个思路，我使用的是华为arkts进行开发的
一号任务整体开发思路（Native 关键词提取 NAPI + ArkTS 鸿蒙）
一、核心目标梳理
你要做一套C++ NAPI 原生文本分析模块，给 ArkTS 业务层提供关键词提取、句子打分能力，替换原有 JS 逻辑；全程基于鸿蒙 NDK、CMake 构建 NAPI 插件，分为「C++ 底层算法→NAPI 绑定→ArkTS 封装→业务层替换」四层流程。依赖前置：项目已配置好 NDK 环境，能正常编译 cpp，CMake 可被 build-profile.json5 识别。
二、分阶段开发流程（按顺序执行，避免编译报错）
阶段 1：工程构建配置（先做，否则 cpp 文件无法编译）
1.新建 entry/src/main/cpp 目录，创建 CMakeLists.txt
配置最小 NDK 版本、目标 so 库名；
把后续 text_analyzer.cpp、napi_init.cpp 加入编译源文件；
引入 napi、libuv 系统依赖库，链接标准 c++ 库支持字符串、哈希计算。
2.修改 build-profile.json5补充 native 编译配置：指定 cmake 路径、编译架构（arm64-v8a）、开启 NDK 构建，让编译流程自动执行 CMake 生成 so。
验证点：配置完成后点击构建，无 CMake、NDK 路径报错再往下写代码。
阶段 2：C++ 底层算法实现 text_analyzer.cpp（核心算法层）
模块提供两大核心能力：中文 n-gram 分词、TF-IDF 权重、TextRank 句子打分。
1.基础工具封装
字符串切割、中英文混合分词（2-gram、3-gram 拆分中文词汇）；
停用词简易过滤（可内置高频停用词数组）；
2.TF-IDF 计算逻辑
统计单文档词频 TF；
简易全局逆文档频率 IDF（实验级轻量化实现，适配笔记短文场景）；
按 TF-IDF 降序筛选 TopN 关键词；
3.TextRank 句子评分
文本分句，计算句子相似度矩阵；
迭代计算句子权重，输出高分摘要句；
4.对外 C++ 函数：extract_keywords、score_sentences，入参输出用标准 string。
阶段 3：NAPI 绑定层 napi_init.cpp（打通 C++ ↔ ArkTS）
这是 Native 与 TS 交互桥梁，核心是注册 NAPI 方法，类型转换（JS 字符串 / 数组 ↔ C++ string/vector）
1.实现两个 NAPI 导出函数
ExtractKeywords：接收原文、关键词数量，调用 text_analyzer 算法，把 C++ 关键词数组转为 JS 数组返回；
ScoreSentences：接收全文，返回带权重的句子数组；
2.类型转换工具封装
napi_value 转 std::string；
vector<string> 转 napi 数组；
异常捕获：文本为空、参数非法时返回报错信息；
3.模块注册入口 RegisterTextAnalyzerModule，后续统一在 NAPI 初始化函数挂载到全局 native 对象。
注意：二号同学后续会修改此文件注册加密模块，你写代码时预留扩展注释，减少合并冲突。
阶段 4：ArkTS 上层封装 NativeTextAnalyzer.ets（工具类，隔离原生细节）
上层业务不直接调用原生 NAPI，统一封装工具类，屏蔽底层异常、参数兼容：
1.加载原生 so 模块：native = requireNapi("libtextanalyzer.so")；
2.封装两个同步工具方法：
typescript
3.
运行
4.
extractKeywords(text: string, topK: number): string[]getSummarySentences(text: string): {sentence: string, score: number}[]
5.
6.统一异常捕获：原生调用失败、空文本兜底返回空数组，提供默认参数（如默认提取 5 个关键词）；
7.注释说明依赖本 Native 模块，方便后续联调。
阶段 5：业务层改造 NoteService.ets（替换原有 JS 实现）
原有业务是纯 JS 关键词提取，全部替换为你封装的 Native 工具：
1.导入 NativeTextAnalyzer；
2.重构 extractKeywords() 函数：删除旧 JS 分词逻辑，直接调用原生工具；
3.重构 buildLocalSummary()：用 TextRank 高分句子生成本地摘要；
4.增加兼容判断：原生模块加载失败时降级回旧 JS 逻辑（兜底容错，防止打包崩溃）；
5.打印调试日志：关键词耗时日志，方便后续五号同学集成测试排查性能。
三、开发自测 & 验证节点（每完成一层就测试，避免堆到最后排错）
1.构建层自测：Sync 项目 → Build HAP，确认 libtextanalyzer.so 成功打包进 hap；
2.C++ 算法自测：可临时写 main 测试函数，输入一段中文笔记，打印关键词结果，验证分词、排序正确性；
3.NAPI 层自测：写临时测试页面，直接调用 native 导出方法，看能否正常返回数组，无崩溃；
4.封装层自测：调用 NativeTextAnalyzer 工具类，校验参数边界（空字符串、超长文本、topK=0）；
5.业务自测：打开笔记页面，新建笔记查看自动生成关键词、摘要是否正常渲染。
四、和 2、3 号同学协同注意点（减少代码冲突）
1.CMakeLists.txt：你先提交基础编译配置，2 号同学新增 crypto_utils.cpp 时仅追加源文件，不要覆盖你的配置；
2.napi_init.cpp：你预留注释 // 此处扩展其他native模块，2 号在此下方注册加密接口，代码分区清晰；
3.文件约定：你所有新建 cpp、ets 工具独立命名，不改动 3 号响应式相关 Constants、页面布局代码，完全解耦；
4.提交规范：分多次提交，先提交 cmake/cpp 底层，再提交 ets 封装，最后提交 NoteService 业务修改，方便代码合并。
五、潜在坑点提前规避
1.NAPI 类型转换错误：数组、字符串转换漏判空，会直接应用闪退，必须加 napi 异常校验；
2.C++ 内存泄漏：字符串、vector 局部变量不要返回裸指针，全部值传递；
3.编译架构不匹配：真机 / 模拟器架构和 NDK 编译架构不一致，so 加载失败；
4.长文本性能问题：笔记超长文本增加截断逻辑，避免 Native 计算阻塞主线程；
5.打包缺失 so：build-profile.json5 配置遗漏，导致真机运行提示找不到 native 模块。
六、交付验收标准（对应任务交付物）
1.新建文件齐全：CMakeLists、text_analyzer.cpp、napi_init.cpp、NativeTextAnalyzer.ets；
2.修改文件：build-profile.json5、NoteService.ets 改动完整；
3.功能可用：笔记模块自动调用 C++ 原生提取关键词、生成摘要；
4.无编译告警、运行无闪退；
5.代码注释完整，标注接口作用、参数含义，方便小组其他人阅读集成。
需要我给你一份可直接复制的基础 CMakeLists.txt 模板和最简 NAPI 初始化代码框架吗？
好的我现在已经完成了工程的构建配置，现在准备开发第二阶段的任务C++底层算法实现
第二阶段 C++ 底层算法开发完整落地思路（text_analyzer.cpp）
一、文件整体结构规划
文件分为 4 块，自上而下编写：
1.头文件引入（NAPI、字符串、容器、算法、数字计算库）
2.全局工具函数：中文 n-gram 分词、停用词过滤、分句工具
3.TF-IDF 关键词提取核心逻辑
4.TextRank 句子打分摘要逻辑对外仅暴露两个给 NAPI 调用的入口函数：vector<string> extractKeywordsCore(string content, int topK)vector<pair<string, double>> scoreSentencesCore(string content)
二、前置依赖头文件（文件最顶部）
cpp
运行
#include <string>#include <vector>#include <unordered_map>#include <unordered_set>#include <algorithm>#include <cmath>#include <sstream>#include <regex>using namespace std;
三、工具函数分步实现
1. 中文分句函数（给 TextRank 使用）
按 。！？ 切分句子，过滤空句
cpp
运行
vector<string> splitSentence(const string &text) {
    vector<string> res;
    string tmp;
    for (char c : text) {
        tmp += c;
        if (c == '。' || c == '！' || c == '？') {
            if (!tmp.empty()) res.push_back(tmp);
            tmp.clear();
        }
    }
    if (!tmp.empty()) res.push_back(tmp);
    return res;}
2. 极简停用词库（笔记场景高频无意义词）
cpp
运行
unordered_set<string> stopWords = {
    "的", "了", "是", "我", "有", "和", "就", "都", "而", "及", "与",
    "这", "那", "也", "还", "不", "很", "非常", "可以", "需要", "如果"};
3. N-Gram 分词（中文 2/3gram 拆分，核心分词逻辑）
单字无意义，只输出 2 字、3 字词；过滤停用词
cpp
运行
vector<string> wordSegment(const string &text) {
    vector<string> words;
    int len = text.size();
    // 2-gram
    for (int i = 0; i < len - 1; i += 3) {
        // 鸿蒙C++字符串单汉字占3字节简化处理
        string w = text.substr(i, 6);
        if (stopWords.find(w) == stopWords.end()) {
            words.push_back(w);
        }
    }
    // 3-gram
    for (int i = 0; i < len - 2; i += 3) {
        string w = text.substr(i, 9);
        if (stopWords.find(w) == stopWords.end()) {
            words.push_back(w);
        }
    }
    return words;}
四、TF-IDF 关键词核心函数（对外入口 1）
轻量化实现（单文档简化 IDF，适配笔记单文本场景）逻辑：分词 → 统计 TF 词频 → 计算权重 → 降序取 TopK
cpp
运行
vector<string> extractKeywordsCore(string content, int topK) {
    if (content.empty() || topK <= 0) return {};
    vector<string> words = wordSegment(content);
    if (words.empty()) return {};

    // 1. 统计TF
    unordered_map<string, int> tf;
    for (auto &w : words) tf[w]++;

    // 2. 简化IDF：文档总数固定为10000（模拟全局语料，实验够用）
    int totalDoc = 10000;
    unordered_map<string, double> weight;
    for (auto &item : tf) {
        string word = item.first;
        int freq = item.second;
        double idf = log((double)totalDoc / (freq + 1));
        weight[word] = freq * idf;
    }

    // 3. 按权重降序排序
    vector<pair<string, double>> wordWeight(weight.begin(), weight.end());
    sort(wordWeight.begin(), wordWeight.end(), [](auto a, auto b) {
        return a.second > b.second;
    });

    // 4. 截取topK关键词
    vector<string> result;
    int takeCnt = min(topK, (int)wordWeight.size());
    for (int i = 0; i < takeCnt; i++) {
        result.push_back(wordWeight[i].first);
    }
    return result;}
五、TextRank 句子打分函数（对外入口 2）
逻辑：分句 → 句子相似度计算 → 迭代收敛权重 → 返回句子 + 分数
cpp
运行
vector<pair<string, double>> scoreSentencesCore(string content) {
    vector<string> sentences = splitSentence(content);
    int n = sentences.size();
    if (n <= 1) return {{content, 1.0}};

    // 句子词集合
    vector<unordered_set<string>> sentWords;
    for (auto &s : sentences) {
        vector<string> ws = wordSegment(s);
        unordered_set<string> set(ws.begin(), ws.end());
        sentWords.push_back(set);
    }

    // 相似度矩阵
    vector<vector<double>> sim(n, vector<double>(n, 0));
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {
            int inter = 0;
            for (auto &w : sentWords[i]) {
                if (sentWords[j].count(w)) inter++;
            }
            double simVal = (double)inter / (sentWords[i].size() + sentWords[j].size() - inter);
            sim[i][j] = simVal;
            sim[j][i] = simVal;
        }
    }

    // TextRank迭代计算权重 d=阻尼系数0.85
    vector<double> score(n, 1.0);
    double d = 0.85;
    int iterTimes = 20;
    for (int step = 0; step < iterTimes; step++) {
        vector<double> newScore(n, 1 - d);
        for (int i = 0; i < n; i++) {
            double sumSim = 0;
            for (int j = 0; j < n; j++) {
                if (i == j) continue;
                sumSim += sim[j][i] * score[j];
            }
            newScore[i] += d * sumSim;
        }
        score.swap(newScore);
    }

    // 组装返回结果
    vector<pair<string, double>> res;
    for (int i = 0; i < n; i++) {
        res.emplace_back(sentences[i], score[i]);
    }
    // 按分数从高到低排序
    sort(res.begin(), res.end(), [](auto a, auto b) {
        return a.second > b.second;
    });
    return res;}
六、开发顺序 & 自测步骤（建议严格按顺序写）
1.先写头文件、停用词、分句函数，编译看无语法报错
2.实现 wordSegment 分词工具，单独测试一段中文，看输出词汇是否合理
3.实现 extractKeywordsCore，测试：输入长笔记文本，指定 topK=5，观察返回关键词是否贴合文本核心
4.实现 scoreSentencesCore，测试长段落，高分句子应为摘要核心句
5.编译校验：CMake 构建无报错，无变量未定义、类型不匹配告警
七、适配 NAPI 层的关键注意点
1.所有对外函数返回标准 vector<string> / vector<pair<string,double>>，方便后续 napi_init.cpp 做类型转换，不用二次处理数据结构
2.全部入参做判空兜底，避免空字符串进入循环导致崩溃
3.不使用裸指针、全局静态长生命周期容器，防止内存泄漏
4.数字全部用 double 存储权重，和 ArkTS number 类型对齐，转换无精度丢失
八、和二号同学协同预留规范
本文件只做文本分析，不引入加密、哈希相关头文件；所有函数命名统一前缀 xxxCore，区分 NAPI 层封装函数，二号后续新增 crypto 模块不会产生命名冲突。
九、当前阶段常见踩坑点
1.中文字节处理错误：鸿蒙 utf-8 汉字 3 字节，substr 截取长度写错会出现乱码词汇
2.停用词未过滤，关键词全是无意义虚词
3.TF-IDF 分母为 0：加入 + 1 偏移避免除零报错
4.TextRank 迭代次数过少，句子权重区分度差
5.忘记判空，空文本进入逻辑导致程序闪退
写完这套 text_analyzer.cpp 后，下一步我可以给你配套的 napi_init.cpp NAPI 绑定完整代码，直接对接这两个核心函数。
你先给我介绍一下TFIDF算法
TF-IDF 完整通俗讲解（适配你当前笔记关键词提取场景）
一、全称与核心作用
TF-IDF = TF（词频） + IDF（逆文档频率）作用：衡量一个词在某篇文本里的重要程度，用来筛选关键词。简单一句话：一个词在当前笔记里出现很多，但在所有笔记里很少出现 → 这个词就是核心关键词。
二、分拆两个概念
1. TF：Term Frequency 词频
公式：词在文档中出现次数文档总词汇数含义：这个词在当前这篇笔记里出现得多不多。举例子：笔记内容：“鸿蒙 ArkTS 开发，ArkTS 原生 NAPI，ArkTS 布局”
ArkTS 出现 3 次，总词汇数 10 → TF 很高
“的” 出现 1 次 → TF 很低
缺陷：只看 TF 会把 “的、了、是” 这类高频虚词当成关键词，所以需要 IDF 修正。
2. IDF：Inverse Document Frequency 逆文档频率
公式：总文档数包含词的文档数分母 + 1 是平滑处理，防止分母 = 0（从没出现过的词报错）。含义：衡量这个词有多稀有。
如果几乎所有笔记都有 “的”：包含该词文档数极大 → IDF 趋近于 0
如果只有这一篇笔记出现 “NAPI、TF-IDF”：包含该词文档数很小 → IDF 数值很大
三、TF-IDF 最终权重
TFIDF(t,d)=TF(t,d)×IDF(t)权重越高，代表这个词越适合当这篇笔记的关键词。
直观对比示例
文本：一篇讲鸿蒙 NAPI 开发的笔记
1.
词：NAPITF：很高（文中多次出现）IDF：很高（大部分日常笔记不会提到）TF-IDF = 高 × 高 → 权重极高，核心关键词
2.
3.
词：的TF：很高（每段都有）IDF：极低（一万篇笔记全都有）TF-IDF = 高 × 低 → 权重极低，直接过滤
4.
5.
词：吃饭TF：极低（本文只出现一次）IDF：很高（很少笔记提吃饭）TF-IDF = 低 × 高 → 权重低，不是关键词
6.
四、你代码里的简化实现说明（适配鸿蒙笔记小型场景）
真实 TF-IDF 需要遍历全部文档库统计每个词出现过多少篇文档，但你现在只是单篇笔记本地提取关键词，没有数据库存储全部笔记，因此做轻量化简化：
1.固定总文档总数 = 10000（模拟全局语料）
2.用当前文档词频 freq代替 “包含该词的文档数量” 做简易 IDF
cpp
运行
double idf = log((double)totalDoc / (freq + 1));
weight[word] = freq * idf;
逻辑等价简化：在单文本内，出现次数越多，IDF 会被压低一点，天然压制重复虚词，实验作业完全够用。
五、完整计算示例（手动演算）
文本：ArkTS NAPI NAPI 鸿蒙分词后词汇：[ArkTS, NAPI, NAPI, 鸿蒙]总文档数 totalDoc=10000
1.
NAPI：出现 2 次IDF = ln (10000/(2+1)) ≈ ln (3333) ≈ 8.11权重 = 2 × 8.11 = 16.22
2.
3.
ArkTS：出现 1 次IDF = ln (10000/(1+1)) ≈ 8.517权重 = 1 × 8.517 = 8.517
4.
5.
鸿蒙：出现 1 次权重 = 8.517
6.
排序后关键词顺序：NAPI > ArkTS > 鸿蒙，和直觉完全匹配。
六、为什么搭配停用词过滤？
TF-IDF 只能压低高频通用词，但无法完全消除。比如 “可以、需要” 这类词 IDF 不会无限接近 0，仍然会干扰结果，因此在分词阶段直接过滤停用词集合，提前剔除无意义词汇，减少算法计算量、提升关键词准确度。
七、优缺点（写实验报告可以直接用）
优点
1.计算简单，C++ 实现轻量化，移动端无性能压力；
2.无需训练模型，开箱即用，适合本地离线提取关键词；
3.逻辑直观，容易调试、看懂权重排序逻辑。
缺点
1.单纯词匹配，不理解语义：“手机” 和 “移动设备” 会被当成两个完全无关词汇；
2.不考虑词语位置（标题、重点段落词汇权重不会自动加高）；
3.简化版没有全局文档库，精准度略低于完整 TF-IDF。
八、和你项目的结合场景
你在 NoteService.ets 中：用户保存笔记 → 传入完整文本到 C++ extractKeywordsCore → TF-IDF 计算权重 → 返回 Top5 关键词，页面展示笔记标签；后续五号同学做集成测试时，也可以针对不同文本验证 TF-IDF 关键词输出是否符合预期。
我现在已经完成了这一部分基本功能的实现你给我几个测试用的示例我看看功能是否正常
分 3 类测试用例：关键词提取 (extractKeywordsCore)、句子打分 (scoreSentencesCore)，覆盖正常文本、边界异常、业务场景（鸿蒙笔记场景）
一、通用测试说明
调用方式：
1.extractKeywordsCore(文本, 提取数量topK) 返回关键词数组
2.scoreSentencesCore(文本) 返回「句子 + 权重」有序列表，权重越高越适合做摘要
测试用例 1：鸿蒙开发笔记（核心业务场景，正常长文本）
输入文本：
plaintext
我最近在用ArkTS和NAPI开发鸿蒙笔记App，使用C++编写TF-IDF关键词提取模块。CMake配置NDK编译Native代码，通过NAPI接口让ArkTS调用底层分词算法。NAPI可以打通C++与前端页面，提升文本处理速度，避免JS大文本卡顿。
1）关键词测试
调用 extractKeywordsCore(上文, 5)预期输出关键词排序：NAPI、ArkTS、C++、TF-IDF、Native逻辑验证：高频且小众专业词汇权重最高，“的、用、和” 等虚词被过滤
2）摘要句子测试
调用 scoreSentencesCore(上文)权重最高句子：使用C++编写TF-IDF关键词提取模块。NAPI可以打通C++与前端页面，提升文本处理速度，避免JS大文本卡顿。
测试用例 2：日常普通笔记（混合长短句）
输入文本：
plaintext
今天学习鸿蒙响应式布局，断点工具区分手机和平板。平板使用SideBarContainer双栏分栏，手机单列展示笔记。写完Native加密模块后要做集成测试，统一UI尺寸常量，修复页面适配bug。
关键词 top5 预期：
响应式布局、SideBarContainer、Native 加密、断点、集成测试
高分摘要句：
平板使用SideBarContainer双栏分栏，手机单列展示笔记。
测试用例 3：边界用例 1—— 极短单句文本
输入：鸿蒙NAPI原生开发
1.extractKeywordsCore (text,3)预期输出：["鸿蒙","NAPI","原生开发"]
2.scoreSentencesCore：仅返回当前句子，分数 1.0
测试用例 4：边界用例 2—— 纯停用词（无有效词汇）
输入：的了是也很可以需要如果和与这那都
1.关键词返回空数组 []
2.句子打分返回原文本，无分词词汇不干扰排序
测试用例 5：边界用例 3—— 空字符串（容错校验）
输入：""（空文本）两个函数全部返回空，无崩溃、无内存异常
测试用例 6：边界用例 4—— 超大长文本（模拟长篇笔记）
输入：
plaintext
ArkTS是鸿蒙主力开发语言。ArkTS兼容TypeScript，提供UI组件、状态管理。NAPI允许开发者使用C/C++编写高性能原生模块。本次项目使用NAPI实现文本分析与密码加密。TF-IDF算法提取笔记关键词，TextRank抽取摘要。响应式断点适配多设备，平板分栏架构优化大屏体验，最后使用HapTest完成全应用集成测试。
提取 top6 关键词预期：ArkTS、NAPI、TF-IDF、TextRank、响应式断点、分栏架构
测试用例 7：混合中英文笔记
输入：
plaintext
开发Note App，使用TF-IDF实现关键词，SHA256做用户密码加密，MediaQuery监听屏幕宽度切换Grid布局。
关键词预期：TF-IDF、SHA256、MediaQuery、Grid 布局、Note App
自测验证标准（出现下面情况代表代码有 bug）
1.输出包含 “的、了、也” 等停用词 → 分词过滤逻辑失效
2.空文本 / 纯虚词返回乱码、程序闪退 → 缺少判空兜底
3.专业低频词汇权重低于普通常用词 → TF-IDF 计算公式写错
4.TextRank 多个句子分数完全一致 → 相似度矩阵 / 迭代逻辑错误
5.中文出现乱码字符（截断半个汉字）→ utf-8 汉字字节截取逻辑错误
快速自测方案
你可以在text_analyzer.cpp临时写一段测试 main 代码（调试阶段用，提交前删除），直接打印结果：
cpp
运行
#include <iostream>void TestDemo() {
    string testText = "我最近在用ArkTS和NAPI开发鸿蒙笔记App，使用C++编写TF-IDF关键词提取模块。";
    vector<string> words = extractKeywordsCore(testText, 5);
    cout << "关键词：";
    for(auto &w : words) cout << w << " ";
    cout << endl;

    auto sentences = scoreSentencesCore(testText);
    cout << "摘要句子：" << sentences[0].first << " 权重：" << sentences[0].second << endl;}
在编译运行后控制台就能直观看到输出结果，对照预期判断算法是否正常。

```

### 10.5 开发辅助 AI 与应用内 AI 功能的区别

开发辅助 AI 是开发阶段使用的豆包，用于帮助开发者理解任务、设计方案、解释算法和准备测试用例。它不会被打包进应用，也不会在用户使用应用时直接运行。

应用内 AI 功能是本项目实际实现并提供给用户的功能，包括本地 AI 自动提取、云端 AI 整理、关键词推荐、摘要生成和待办事项提取等。应用内 AI 功能运行在 HarmonyOS 应用中，通过 ArkTS、Native C++ 模块和相关业务逻辑共同完成。
## 十一、总结

本项目完成了一个面向手机和平板的 HarmonyOS 灵感笔记应用。基础功能上，应用支持用户注册登录、笔记管理、待办管理、搜索筛选、图片选择和 AI 整理。Native 增强方面，项目将中文关键词提取、句子评分和密码加密迁移到 C++，通过 NAPI 被 ArkTS 调用，体现了跨语言协同开发能力。跨设备适配方面，项目通过断点系统和 SideBarContainer 实现手机单栏、平板多栏和平板列表-详情双栏，避免了简单拉伸。

开发过程中解决了中文分词、Native ABI、NAPI 宏冲突、ArkUI 回调属性、平板列表刷新、多待办预览、无待办场景误生成等问题。测试方面，项目完成了本机 HAP 构建验证、Native 密码加密验证、手机和平板模拟器验证，并整理了 HapTest 主链路测试方案。后续可继续完善自动化真机测试记录、Native `.d.ts` 类型声明、云 AI 密钥安全管理和本地词典外置能力。

